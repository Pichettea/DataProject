use fit4life;

CREATE TABLE Users (
    Username VARCHAR(255),
    FirstName VARCHAR(255),
    LastName VARCHAR(255),
    Age INT,
    Gender VARCHAR(50),  -- This is where the gender data is stored
    PhoneNumber VARCHAR(15),
    UserPassword VARCHAR(255)
);